/**
 * VoiceStatusBar — the voice status is now embedded directly in the
 * ChannelSidebar under the Channels section. This component is kept as
 * a no-op so nothing breaks, but it no longer renders the floating pill.
 */
export function VoiceStatusBar() {
  return null;
}
